# Frontend Environment Variables
# Copy this file to .env.local for local development
# In production (AWS Amplify), set these in the Amplify Console

# ============================================
# Backend WebSocket Server URL
# ============================================

# WebSocket Server URL
# This should match the backend server URL (PORT 3004 by default)
# Development: http://localhost:3004
# Production: Your AWS App Runner URL (e.g., https://xxxxx.ap-northeast-1.awsapprunner.com)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3004

# Note: This variable MUST start with NEXT_PUBLIC_ to be accessible in the browser
# The backend URL should match the ALLOWED_ORIGINS setting in the backend .env file
