# Backend Environment Variables
# For local development, create .env file
# In production (AWS App Runner), set these in the App Runner Console

# Server Port (AWS App Runner will set this automatically)
PORT=3004

# Allowed CORS Origins (comma-separated)
# Development: http://localhost:3000
# Production: Add your Amplify URL (e.g., https://main.xxxxx.amplifyapp.com)
ALLOWED_ORIGINS=http://localhost:3000

# Node Environment
NODE_ENV=development
